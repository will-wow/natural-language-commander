{
  "name": "natural-language-commander",
  "version": "0.0.5",
  "description": "A tool for connecting natural language commands with callbacks.",
  "homepage": "https://github.com/whenther/natural-language-commander",
  "bugs": "https://github.com/whenther/natural-language-commander/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/whenther/natural-language-commander.git"
  },
  "main": "dist/nlc.js",
  "scripts": {
    "test": "mocha --opts ./mocha.opts",
    "build": "typings install && tsc && browserify -o dist/nlc.js -e dist/NaturalLanguageCommander.js --standalone nlc && uglifyjs dist/nlc.js -o dist/nlc.min.js",
    "prebuild": "rimraf dist",
    "postbuild": "rimraf dist/test",
    "prepublish": "npm run build"
  },
  "keywords": [
    "natural language processing",
    "nlp",
    "commands",
    "bots",
    "hubot"
  ],
  "author": "Will Lee-Wagner <will@whentheresawill.net>",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^13.0.1",
    "chai": "^3.5.0",
    "chai-spies": "^0.7.1",
    "mocha": "^2.5.1",
    "rimraf": "^2.5.2",
    "ts-node": "^0.7.3",
    "typescript": "^1.9.0-dev.20160531-1.0",
    "typings": "^1.0.4",
    "uglify-js": "^2.6.2",
    "uglifyify": "^3.0.1"
  },
  "dependencies": {
    "es6-promise": "^3.2.1",
    "lodash": "^4.6.1",
    "moment": "^2.12.0",
    "moment-timezone": "^0.5.3"
  },
  "typings": "./dist/NaturalLanguageCommander.d.ts",
  "engines": {
    "node": "5.10.x"
  }
}
